"""RTMemory MCP Server — Model Context Protocol integration.

Exposes RTMemory operations as MCP tools so AI agents can
add memories, search, manage profiles, etc. via the MCP protocol.

Run: python -m app.mcp.server

Requires: mcp package (pip install mcp)
"""